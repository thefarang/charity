'use strict'

// Describes the shape of the UI schema and it's corresponding
// constraints
const CauseAuthSchema = {
  cause_name: 'cause_name',
  cause_country: 'cause_country'
}

module.exports = CauseAuthSchema
